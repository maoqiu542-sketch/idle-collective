{
  "workflow_id": "terrain_workflow",
  "category": "terrain",
  "description": "ComfyUI prompt template for large-shape terrain tiles.",
  "template_version": "1.0.0",
  "api": {
    "checkpoint": "{{checkpoint}}",
    "positive_prompt": "{{prompt}}",
    "negative_prompt": "{{negative_prompt}}",
    "width": "{{width}}",
    "height": "{{height}}",
    "steps": "{{steps}}",
    "cfg": "{{cfg}}",
    "sampler": "{{sampler}}",
    "scheduler": "{{scheduler}}",
    "seed": "{{seed}}",
    "batch_size": 1
  }
}
